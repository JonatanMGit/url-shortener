global:
  resolve_timeout: 5m

route:
  receiver: webhook
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 30m

receivers:
  - name: webhook
    webhook_configs:
      - url: http://host.docker.internal:9080/alerts
        send_resolved: true
